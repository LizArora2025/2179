{
  "$schema": "https://vega.github.io/schema/vega-lite/v5.json",

  "title": "Shark Incidents per Million Residents by State",

  "width": 650,
  "height": 350,

  "data": {
    "url": "Australian_Shark_Incidents_Merged.csv",
    "format": {
      "type": "csv"
    }
  },

  "transform": [
    {
      "aggregate": [
        {
          "op": "count",
          "as": "Incidents"
        },
        {
          "op": "max",
          "field": "Population_2025_000",
          "as": "Population"
        }
      ],
      "groupby": ["StateName"]
    },

    {
      "calculate": "toNumber(datum.Population)",
      "as": "PopulationNum"
    },

    {
      "calculate": "(datum.Incidents / datum.PopulationNum) * 1000",
      "as": "IncidentsPerMillion"
    }
  ],

  "mark": {
    "type": "bar",
    "color": "#005f73"
  },

  "encoding": {
    "y": {
      "field": "StateName",
      "type": "nominal",
      "sort": "-x",
      "title": "State"
    },

    "x": {
      "field": "IncidentsPerMillion",
      "type": "quantitative",
      "title": "Shark Incidents per Million Residents"
    },

    "tooltip": [
      {
        "field": "StateName",
        "title": "State"
      },
      {
        "field": "PopulationNum",
        "title": "Population (000s)"
      },
      {
        "field": "Incidents",
        "title": "Total Incidents"
      },
      {
        "field": "IncidentsPerMillion",
        "format": ".2f",
        "title": "Incidents per Million"
      }
    ]
  }
}
